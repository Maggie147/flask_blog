# -*- coding: utf-8 -*-
import hashlib

# def __hashed_with_salt(info, salt):
#     m  = hashlib.md5
#     m.update(info.encode('utf-8'))
#     m.update(salt)
#     return m.hexdigest

# def hashed_login_pwd(pwd):
#     return __hashed_with_salt(pwd, const.login_pwd_salt)
